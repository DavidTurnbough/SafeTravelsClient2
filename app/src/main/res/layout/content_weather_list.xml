<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:focusable="false"
    android:focusableInTouchMode="false"
    tools:context="com.example.safetravelsclient.models.WeatherListActivity"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">

    <ListView
        android:id="@+id/content_weather_listing"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10px"
        android:divider="#000"
        android:dividerHeight="1px"
        android:focusable="false"
        android:focusableInTouchMode="false"
        android:descendantFocusability="blocksDescendants">
    </ListView>

    <Button
        android:id="@+id/back_button_weather_list"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="30dp"
        android:layout_marginTop="20dp"
        android:text="Map"
        app:layout_constraintStart_toEndOf="@+id/content_weather_listing"
        android:layout_below="@+id/content_weather_listing"/>

</RelativeLayout>